version: 2

sources:
  - name: raw
    database: retail-weather-pipeline
    schema: raw
    tables:
      - name: weather
        description: "Raw daily weather data loaded from GCS, one row per city per date."
