retail_weather:
  outputs:
    dev:
      dataset: raw                          # fallback only — +schema in dbt_project.yml controls actual dataset routing
      job_execution_timeout_seconds: 300
      job_retries: 1
      keyfile: "{{ env_var('BIGQUERY_KEYFILE_PATH') }}"
      location: us-central1
      method: service-account
      priority: interactive
      project: retail-weather-pipeline
      threads: 4
      type: bigquery
  target: dev
